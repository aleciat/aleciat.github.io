Dispatching action for the filter:

filter: "SHOW_ALL"
type: SET_VISIBILITY_FILTER



<div className="mainPosts" key={post.id} id={post.id}>
		<div className="individualPost">
		<div className="postTitle">{post.title}</div>
		<div className="postAuthor">Author: <em>{post.author}</em></div>
		<div className="postCategory">{post.category}</div>
		</div>
		<div className="upvotesColumn">{post.voteScore}</div>
		<div className="timestampColumn">{post.timestamp}</div>
        </div>
		
		
		
		<div className="addNewButton"><Link to="/create"><button className="addNew">Add New Post</button></Link></div>
		
		
			
renderPosts = () => {
	return this.props.posts.map(post => this.renderPostPreview(post));
};
  
renderPostPreview = (post) => {
	let structuredDate = new Date(post.timestamp);
    return (
      
);
}; 
	
render() {
const renderingPosts = this.renderPosts();
  console.log(this.props)
  

return (
<div>
{renderingPosts}
</div>
)

	

}

  {fireRedirect && (
          <Redirect to={'/thanks'}/>
        )}
	


